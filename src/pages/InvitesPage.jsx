import { ComingSoonPage } from './ComingSoonPage'

export default function InvitesPage() {
  return (
    <ComingSoonPage
      flowKey="invites"
      note="Pre-approve visitors with a QR e-pass, auto-expiry, and a per-host daily limit."
    />
  )
}
