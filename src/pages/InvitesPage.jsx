import { FiMail } from 'react-icons/fi'
import { ComingSoonPage } from './ComingSoonPage'

export default function InvitesPage() {
  return (
    <ComingSoonPage
      icon={FiMail}
      title="Invites — coming next"
      description="Pre-approve visitors with a QR e-pass, auto-expiry, and a per-host daily limit."
    />
  )
}
