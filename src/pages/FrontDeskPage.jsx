import { FiMonitor } from 'react-icons/fi'
import { ComingSoonPage } from './ComingSoonPage'

export default function FrontDeskPage() {
  return (
    <ComingSoonPage
      icon={FiMonitor}
      title="Front Desk Dashboard — coming next"
      description="Search/filter the visitor list, guest detail panel, check-in/out, and overstay flagging."
    />
  )
}
